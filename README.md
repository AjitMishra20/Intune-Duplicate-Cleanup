# Intune-Duplicate-Cleanup
Intune duplicate device cleanup scripts using Microsoft Graph. Detects duplicates by Serial Number and removes stale duplicate Intune records while keeping Entra device objects for audit.
